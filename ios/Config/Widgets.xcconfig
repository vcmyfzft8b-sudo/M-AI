#include "Version.xcconfig"
// The extension's identifier must be prefixed by the app's, and it carries no
// entitlements of its own — Sign in with Apple belongs to the app alone.
PRODUCT_BUNDLE_IDENTIFIER = eu.memoai.memo.widgets
// Set DEVELOPMENT_TEAM in Local.xcconfig (ignored) or Xcode Signing & Capabilities.
#include? "Local.xcconfig"
