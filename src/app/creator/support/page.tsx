import { SupportScreen } from "@/components/support-screen";

/** The demo runs the real help screen, against the demo's own article routes. */
export default function CreatorDemoSupportPage() {
  return <SupportScreen basePath="/creator/support" />;
}
