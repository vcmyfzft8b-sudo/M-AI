export const BRAND_NAME = "Memo";
export const SEO_BRAND_NAME = "Memo AI";

/** Combined brand lockup: the brain mark and the "Memo AI" wordmark in one asset. */
export const BRAND_LOCKUP_SRC = "/memo-lockup.png";
export const BRAND_LOCKUP_WIDTH = 480;
export const BRAND_LOCKUP_HEIGHT = 148;
export const BRAND_SUPPORT_EMAIL = "info@memoai.eu";
export const SEO_SITE_URL = "https://memoai.eu";

/*
 * The tagline, the short line under the title and the site description used to
 * live here as constants. They are copy, not brand identity, and Memo now
 * ships in five languages — so they moved into the message catalogues as
 * `meta.tagline`, `meta.shortline` and `meta.description`. The name, the
 * lockup, the support address and the canonical URL are the same in every
 * language and stay.
 */
